# Claude Code への引き継ぎ

このリポジトリはブラウザで動くバトルロイヤルゲーム。**このファイルを最初に読むこと。**

## 大前提

- **外部ライブラリを追加しない。** 素のJavaScript、Canvas 2D、CSSだけで作る。npm も build も使わない
- **ファイルを増やさない。** ゲームロジックは `js/game.js` 一本にまとめる。分割したくなっても分割しない
- **`localStorage` / `sessionStorage` を使わない**
- UIの文言はすべて日本語
- 実験モード（パスワード `monoshirikunn`）は検証用。**通常プレイに影響を出さないこと**。`expMode` の分岐だけで完結させる
- 仕様の正は `docs/SPEC.md`。**コードを変えたら SPEC.md も同じコミットで直す**

## 変更したら必ず実行する

```bash
node tools/sim.js 5
```

ブラウザなしで全500種の武器を発射し、5試合を最後まで自動対戦させる。
例外が出るか試合が決着しないと終了コード1で落ちる。**これが通らないコードはコミットしない。**

さらに、見た目に関わる変更をしたらブラウザで実際に1試合遊んで確認すること。

## コードの地図（js/game.js）

上から順に、以下のブロックが並んでいる。

| 行のあたり | 内容 |
|---|---|
| 冒頭 | ユーティリティ、`RANKS`（7ランク）|
| `BASES` | 基礎武器70種。ここに1つ足すと自動で7ランク分＝7種増える |
| `SPECIALS` | 特別武器10種（キャラ初期武器6＋ボスドロップ4） |
| `CHARS` / `BOSSES` | キャラ6人、ボス4体の定義 |
| `genWorld()` | フィールド生成。予約マスク `res` で家と高台の重複を防いでいる |
| `walkable/tileFree/moveEnt` | 壁の当たり判定と高低差の処理。**ここは繊細なので触るなら慎重に** |
| `newEnt/spawnAll` | 100人の生成 |
| `hurt/kill/fire/meleeHit` | ダメージと攻撃 |
| `updateBullets/onHit/impact` | 弾の移動・命中・爆発 |
| `pathStep` | 終盤（残り6人以下）だけ使う幅優先探索。壁を回り込ませる |
| `botAI` | CPUの思考 |
| `thunderStrike/updateStrikes` | 壁も段差も無視する落雷。共通の `strikes` 配列 |
| `updateHazards` | 炎の海など継続ダメージの床＋落雷の更新。**メインループから毎フレーム呼ぶ** |
| `aimPoint(e,maxd)` | 狙っている地点。**向きは必ず `e.ang`**、距離だけ照準や相手から取る |
| `drawBossShape` | ボス4体の見た目。前方が上向きの状態で描く |
| `updateBoss/bossAbility/bossDie` | ボス本体・ビーム・4体それぞれの特殊能力 |
| 入力 → `draw` → `drawHUD` | 描画とUI |
| `loop` | メインループ |
| 末尾 | メニュー画面、武器図鑑、実験モードのパスワード判定、`init()` |

## よくある作業のやり方

**武器を増やす** → `BASES` に `M(...)` か `R(...)` を1行足すだけ。図鑑もドロップも自動で対応する。
新しい効果を足す場合は `meleeHit` / `onHit` / `impact` のどれかに処理を書き、SPEC.mdの効果一覧にも追記する。

**ビーム中は動かないし回らない** → 照射中に移動や旋回をさせるとビームが避けられなくなり、反撃の機会も消える。
`b.beamAng` は予告が始まる瞬間に一度だけ決める。**予告中も照射中も追尾させないこと**（追尾を足すと避けられなくなる）。
また、当たった相手を横へ押し出す処理を消さないこと。**スタン中に照射されると動けず、押し出しがないと逃げ場がなくなる**。

**チャンスタイムを壊さない** → 特殊能力は30秒周期で、使った直後の10秒は `b.rest` が立ち、
`updateBoss` の頭で何もせず `return` する。`b.abCd` は `b.rest` 中も減らすこと（減らさないと周期が40秒になる）。
能力を発動したフレームはそのまま `return` する（しないとその1フレームだけ動いてしまう）。

**ボスの能力を足す** → `BOSSES` の `ab` に種別名を書き、`bossAbility()` に分岐を1つ足す。
共通のビームと弾幕は `updateBoss` 側にあるので触らなくてよい。
見た目はまだ仮（色付きの丸）なので、設計者の指示が来るまで作り込まない（docs/TODO.md参照）。

**バランス調整** → 数値をいじったら `node tools/sim.js 10` で平均試合時間を見る。
**6〜9分に収まり、全試合が決着していれば健全**。未決着が出たらCPUがどこかでハマっている。

## 過去に踏んだ罠（同じ失敗を繰り返さないこと）

1. **家が3軒しか建たなかった** — 岩と高台を先に置いたせいで空き地がなくなった。生成順は「予約 → 家 → 高台 → 岩」を守る
2. **開始1分で60人死んだ** — 全員が虹ランクの初期武器を持つため、初期索敵範囲を240pxまで狭めた
3. **最後の2〜3人が延々と決着しなかった**。原因は3つあり、全部直してある。逆に言うとここを触ると再発する
   - 高台と地上で近接が当たらず永久に睨み合っていた → 72px以内なら段差越しでも当たるようにした
   - 弾切れの銃を構えたまま距離を取り続けていた → 素手（`BARE`）を追加した
   - 壁越しに敵を追い続けて詰まっていた → 1.4秒動けなければ迂回モードに入るようにした
4. **CPUが高台に閉じ込められた** — 詰まりが続いたら最寄りの階段（`stairsList`）を目指す
5. **ボスが勝手に減っていった** — 弾幕がボスの中心から出るため、発射直後に自分に当たっていた。
   `updateBullets` のボス判定で `bs===b.o` を除外し、`aoeHit` でも `b!==src` を確認し、弾は体の外側から出す。
   同じ形の判定を増やすときは**必ず発射者本人を除外すること**
6. **弾が射程の16倍飛んでいた** — `life:(d.rng/spd)*16` の `*16` が誤り。`life` は「残りフレーム数」で、
   毎フレーム `dt/16`（≒1）引かれる。射程を変えるときはこの単位に注意
7. **弾幕系の武器でエフェクトが際限なく増えた** — `addFx` は760件、`addText` は90件で打ち切る。
   ターボマシンガンHULLは毎秒540発撃つので、上限を外すと確実に処理落ちする
8. **アイテム拾い判定が毎フレーム全ドロップ（最大500件）を走査していた** — CPUは140ms、
   武器拾いは200msごとに間引いてある。ここを毎フレームに戻さないこと
9. **ボスが中央の高台に立っていて、地上からの弾が全部段差で止まっていた** — 爆発系しか当たらない状態だった。
   闘技場は平らにしてある。**段差を戻さないこと**。闘技場の宝箱も真横に置くと射線を塞ぐので斜めに置いてある
10. **武器スロットを2個固定で書いていた** — 実験モードは10個。`e.wp[1-e.cur]` のような書き方はもう通用しない。
   `curW()` `otherW()` `takeWeapon()` を使い、スロット数は `e.wp.length` から取ること
11. **狙い先をカーソル座標から直接計算したら、向きと食い違った** — `aimPoint` は向きを `e.ang` に固定し、
   距離だけカーソルから取る。ヘッドレステストでは `cam` と `mouse` が原点のままなので、
   照準を使う技を試すときはテスト側で `cam.x=0; mouse.x=狙う世界座標` のように設定すること
12. **プレイヤーを不死身にしてテストしたら結果が狂った** — CPUが倒せない的に群がるため。`tools/sim.js` は
   プレイヤー枠も `player.bot = 1` にしてCPUとして動かしている。この作りを変えないこと
